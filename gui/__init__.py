"""交易世界 · GUI 桌面端。

架构（为什么这么选）
--------------------
    前端：单个 HTML 文件（HTML+CSS+JS 全内联，**不加载任何 CDN**）
    后端：Python 标准库 http.server（不引入 Flask 等第三方框架）
    窗口：pywebview（Windows 上用系统自带的 WebView2，无需打包浏览器）

三条理由：

1. **零构建步骤。** 没有 npm / webpack / 打包器。改完代码刷新即生效，
   也不需要联网（这台机器可能离线）。图表用手写 canvas 画，不引 Chart.js。
2. **零额外依赖（除窗口本身）。** httptest 用的是标准库；
   pywebview 只负责"开一个原生窗口指向本地地址"。
   **它缺失时自动退回系统浏览器**——功能完全一样，只是少了原生窗口外壳。
3. **模拟是 Python 写的，后端必须是 Python。** 用 Electron/Tauri 就得跨进程
   传数据或重写模拟，得不偿失。

线程模型
--------
    · HTTP 线程：只做轻量读写，从不跑模拟（否则页面会卡死）
    · 工作线程池：真正跑模拟，分块推进 → 可报进度、可取消
    · GUI 线程：pywebview 的主循环（阻塞）

安全边界
--------
服务只绑定 ``127.0.0.1``，并且每个进程启动时生成一个一次性 token；
所有**写操作**都要带 ``X-TW-Token``。这是本地工具的标准做法，
防的是"浏览器里的其他页面顺手打到这个端口"。

⚠️ 但要说清楚：本应用允许在界面里**粘贴并运行 Python 策略代码**
（这是它的核心功能之一）。那等于在本机执行任意代码。
不要运行来源不明的策略代码。
"""

__version__ = "1.0.0"
