# 三线深挖 · 交付小结

> 对应任务书：`交易世界 · 三线深挖任务书.md`（三条工作线 A / B / C）。
> ⚠️ **本文件由 `scripts/make_workstream_summary.py` 生成**，
> 所有数字从 `out/workstream_*_metrics.json` 现算，不手抄。
> 生成时间：2026-09-19 05:37:36

---

## 0. 先说一件比任务书三条线都重要的事

任务书是**看不到本项目源码**的情况下、凭二期报告写的，
所以它的伪代码与假设有 **8 处**与实现不符。按本项目
「每个 patch 下笔前锚定真实源码行」的规矩，动手前先做了前提核对。
**其中 1 处若照抄会让机制静默失效**（见下方 B 线）。
完整核对表在 `.workbuddy/memory/2026-09-18.md` 与本轮的 A.0/B.0/C.0 结论里。

---

## 1. 工作线A：阶段8「意外修复」的机制解耦

### 1.0 一句话结论

> **阶段8 宣称的「k 从 1.395 拉到 0.481、离平方根律只差 0.019」是实验口径错误造成的假象。**
> 根因不是任务书设想的任何一个假说，而是：**λ̄ 的标定市场与实际使用的市场不是同一个**。
> 修正标尺后 k 只走到 0.947（而不是 0.481），离 0.5 还差很远。

### 1.1 EA.0 先排除「公式错误」这条通道

`HawkesConfig.alpha = n/(e^{β·dt}−1)`（离散），不是 `n·β`（连续）。实测事件率：

| 分支比 n | 现行（无 clamp）偏差 | 旧公式（α=n·β）偏差 |
|---|---|---|
| 0.0 | +0.02% | +0.02% |
| 0.3 | -0.07% | -2.99% |
| 0.6 | -0.03% | -10.00% |
| 0.9 | +0.96% | -40.07% |

**并附带一次自我纠错**：我曾把「有/无 clamp」的差异（约 6 个百分点）当成护栏效应写进分析，实际那是**两次用了不同随机流**的蒙特卡洛噪声。同种子重测后：

| n | 公式偏差(无 clamp) | clamp 后偏差 | 差（clamp 造成） |
|---|---|---|---|
| 0.0 | +0.02% | +0.02% | +0.00% |
| 0.3 | -0.07% | -0.07% | +0.00% |
| 0.6 | -0.03% | -0.03% | +0.00% |
| 0.9 | +0.96% | +0.96% | +0.00% |

→ **clamp 在本区间完全不生效**（ρ 的量级 0.6~1.4，离阈值 4 很远）。**「护栏压低了活跃度」这个说法是错的，已删除。**

### 1.2 EA.1：k 逐位复现，但**机制其实一直哑火**

用 E8.2 原样口径重跑，k 复现为 1.395 → 0.481（与 E8.2 报出的一致，说明装置无误）。同时测出：

| 臂 | k | ρ̄ | 平均活跃主体 | 稀疏化**真正生效**的占比 | 成交/tick |
|---|---|---|---|---|---|
| Hawkes 关（对照） | 1.395 | 0.895 | — | — | — | 88.50 |
| Hawkes 开(b=0.6) | 0.481 | 0.019 | 1.4756 | 300.0 | 0.0% | 89.73 |
| Hawkes 开(b=0.9) | 0.725 | 0.225 | 1.7197 | 300.0 | 0.0% | 90.01 |

λ̄（E8.2 口径）= 50.093，而该市场真实基线成交率 = 88.495（**标尺偏 1.767×**）。

⇒ ρ̄ ≈ 1.47 ⇒ `round(1.47×300)=441` 被上限夹到 300 ⇒ **每个 tick 都返回全部主体** ⇒ 稀疏化**从未发生**。
而这一点**从产物里完全看不出来**（k 确实变了，极易读成「机制生效但效果有限」）。

### 1.3 EA.1b：修正标尺后，真正的效果有多大

λ̄（同市场口径）= 94.601，标尺偏 0.935×（接近 1 ✅）。

| 臂 | k | \|k−0.5\| | ρ̄ | 平均活跃主体 | 稀疏化生效 | 成交/tick |
|---|---|---|---|---|---|---|
| Hawkes 关（对照） | 1.395 | 0.895 | — | — | — | 88.50 |
| Hawkes 开(b=0.6) | 0.947 | 0.447 | 0.9206 | 276.0 | 94.8% | 81.96 |
| Hawkes 开(b=0.9) | 0.812 | 0.312 | 0.7418 | 222.2 | 95.8% | 67.37 |

⇒ **聚集性真的改善了 k，但幅度只有 E8.2 宣称的一半左右。**

### 1.4 EA.2 突发期深度（H1）

- 相对效应：**-6.52%**（跨运行 t=-2.18，p=0.1609，n=3）
- 结论：**不显著：不能宣称突发期深度与背景期不同**
- 冲击前一刻的盘口深度（与阶段3 `depth_before` 同口径）= 29.5 手
- ⚠️ 统计口径：**以跨运行的检验为准**。把所有 tick 当独立样本的tick 级检验，其区间比诚实的跨运行区间**窄约 11.6 倍**（合成 AR(1) 实测），它抹掉的是「小效应 vs 噪声」的分辨力。

### 1.5 EA.3 方差匹配对照（H2）

做法：把 Hawkes 跑出来的 ρ 序列**随机置换**后喂回去——边际分布（因而均值与方差）完全一致，只把「事件→事件」的自激依赖抹掉。

- ρ 的 lag-20 自相关：Hawkes +0.0471 → 置换代理 +0.0069（**代理确实没有自激了**，这是本实验的前提）
- 置换代理的 k = **0.481**（\|k−0.5\| = 0.019）
- ⇒ **H2 成立，而且比「时间结构」更关键**：一个没有自激、只有相同活跃度波动的过程，反而把 k 拉得更接近 0.5。

### 1.6 EA.4 单侧稀疏化：聚集作用在需求侧还是供给侧？

| 稀疏化作用面 | k | \|k−0.5\| | ρ̄ | 平均活跃主体 | 稀疏化生效 | 成交/tick |
|---|---|---|---|---|---|---|
| both | 0.947 | 0.447 | 0.9206 | 276.0 | 94.8% | 81.96 |
| taker | 0.455 | 0.045 | 0.9199 | 278.4 | 99.2% | 81.89 |
| maker | 0.689 | 0.189 | 0.9221 | 297.7 | 96.2% | 82.44 |

⇒ **需求侧（吃单方）才是关键**：只稀疏化吃单方时 k = 0.455（几乎精确命中平方根律），而两侧同时稀疏化反而互相抵消。

> ⚠️ **后续更正（回填标准配置后，见工作线 L / O）**：
> 三线深挖最初观察到「只稀疏化吃单方」(k=0.455) 比「双边稀疏化」(k=0.947) 更接近平方根律，据此提出「非对称原则」是修复冲击凹度的关键。
> 经过回填标准配置（8 种子、未饱和档位）重新测量，taker / both / maker 三个配置的 k 分别为 **0.734 / 0.794 / 0.739**，**极差只有 0.060**，而这个差异所需的分辨力（约 **1000 个种子**）远超过当前投入的规模（工作线 O 的区间重叠检验：三对两两比较的重叠都在 80% 以上）。
> 更准确的结论是：真正驱动冲击凹度改善的，是**「存在时间聚集性」这件事本身**（Hawkes 关闭→开启，k 从 1.292 到约 0.755，效应量 **0.536**）；聚集性具体作用在**需求侧还是供给侧**，影响相对次要——**效应量小一个数量级**（0.060 vs 0.536，约 9 倍）。
> 注：上面这些点估计**方向仍然一致**（taker 最小），只是统计上无法与噪声区分；被撤回的是「非对称配置更好」这个**结论性**说法，不是这些读数本身。

### 1.7 A 线对三个假说的裁决

| 假说 | 裁决 | 依据 |
|---|---|---|
| H1 突发期局部流动性变薄 | **否定** | EA.2 跨运行检验不显著 |
| H2 方差效应 | **成立，且是主因** | EA.3 置换代理（无自激、同方差）k 比真 Hawkes 更接近 0.5 |
| H3 离散化偏差（公式错） | **否定**（公式实测偏差 < 2%） | EA.0 |
| **（任务书未设想的第四种）实验口径错误** | **成立，是 E8.2 假象的根源** | EA.1/EA.1b：标尺偏 1.74×，机制 99.8% 的时间哑火 |

---

## 1.5 三线合并验证（A+B，以及放进阶段11 场景）

> 合并规则与理由写在 **`docs/三线深挖-合并计划.md`**（先写假设再跑）。

### J1–J3：联合配置下的 k

| 臂 | λ̄（同市场口径） | k | \|k−0.5\| | ρ̄ | 稀疏化生效 | 成交/tick |
|---|---|---|---|---|---|---|
| J1 阶段6 基线 | 90.58 | 1.251 | 0.751 | — | — | — |
| J2 +B 三类覆盖 | 71.84 | 0.765 | 0.265 | — | — | — |
| J3 +A 修正 Hawkes | 71.84 | 0.957 | 0.457 | 1.0319 | 24.4% | 75.75 |

- 阶段6 基线 k = 1.251；**联合配置最终 k = 0.957**（离 0.5 还有 0.457）
- J3 的稀疏化生效占比 = 24.4%（不足 50% 时该臂的 k 不许解释成「聚集性的效果」）
- ⚠️ **C 线对这个 k 的贡献是零，不是「很小」**：C 的修复只作用于配对交易者，而配对交易者在多资产市场里，本 k 检验是单资产实验。

### J4：阶段11「至暗时刻」链条（联合配置）

- 逐资产 λ̄：A=50.89，B=50.67（**不能共用一个 λ̄**——那正是 E8.2 的错误形态）
- **成立环节数 = 4/6**（种子数 3；判据与 E11.1 完全一致，只换配置）

| 环节 | 观察到 / 种子数 | 是否成立 |
|---|---|---|
| ① A 的价格偏离(bp) | 0/3 | ❌ |
| ② B 的价格偏离(bp) | 0/3 | ❌ |
| ③ A 的买盘深度(手) | 2/3 | ✅ |
| ④ 趋势追随者净持仓 | 2/3 | ✅ |
| ⑤ A 的资金费率 | 3/3 | ✅ |
| ⑥ 套利者净持仓 | 3/3 | ✅ |

---

## 2. 工作线C：阶段9 修复后重跑 + 配对反事实

### 2.1 EC.1 修复前后对比（记账类静默 bug 的具体案例）

| 配置 | 组合 PnL (bp) | 腿A成交 | 腿B成交 | 净/总暴露 |
|---|---|---|---|---|
| 修复前（short_limit=0） | +284.772 | 5652 | 9035 | 1.000 |
| 修复后（3×notional） | +482.321 | 10516 | 16984 | 0.226 |

⇒ **bug 造成的 PnL 偏差 = +69.4%**（+284.772 → +482.321 bp）。
⇒ 净暴露占比从「完全单边」降到约两成：**修复前那套「配对交易」其实是方向性策略**。

⚠️ 口径说明：净/总暴露是**运行结束那一刻**的快照。两腿是渐进调仓（`adjust_frac`），所以它取决于运行停在哪一段相位；0.226 这个数说明「大部分时候对冲住了」，不等于「时刻严格中性」。

### 2.2 EC.2 基线回归

- 不用 pairs 的路径在两种 `short_headroom` 下**逐位一致**：✅ 是（修复是局部的，不影响基线路径）

### 2.3 EC.3 E9.4 配对反事实（补上一期三原则第二条）

种子数 12（原来是 3）。做法：对每个 (σ_c, 种子) 跑两次（不砸 A / 砸 A），B 的价格路径**逐 tick 相减**。

| σ_c | 实测相关 | 未配对 B 最大偏离 ± sd | 配对后传导幅度 ± sd | 配对后末段 |
|---|---|---|---|---|
| 3.0e-04 | 0.0013 | 466.3 ± 71.1 | 391.0 ± 115.9 | -1.7 |
| 1.0e-03 | 0.0410 | 528.1 ± 104.7 | 405.0 ± 82.5 | -0.6 |
| 2.0e-03 | 0.0847 | 656.8 ± 138.4 | 402.5 ± 99.6 | +9.2 |
| 8.0e-03 | 0.4159 | 1717.9 ± 665.3 | 391.3 ± 207.8 | -1.1 |

- 配对带来的降噪倍数（跨种子 sd 之比）均值 = **1.62×**
- 配对后传导幅度是否单调：**否**

⚠️ **诚实说明（与任务书的预期不一致）**：任务书期待降噪倍数「显著大于 1，参考一期 62 倍量级」。实测**小于 1**——也就是说配对在这里**没有降噪**。原因不是配对做错了，而是：

1. 本项用的是 `max|偏离|`（**极值统计量**），它本身就是噪声主导的；
2. 冲击对 B 的**真实效应**（数百 bp）比 B 自身的漂移（几十 bp）**更大**，所以扣掉漂移之后，剩下的效应本身在种子间的散布就成了主要方差来源。

一期那 62 倍降噪发生在「效应远小于漂移」的场景里，与这里不是同一情形。**这条差异如实报出，不用「同一数量级」把它糊过去。**

---

## 3. 工作线B：元订单覆盖面扩展

### 3.1 EB.1 覆盖面消融

| 配置 | k | \|k−0.5\| | 元订单数（一场） | 未饱和档位 |
|---|---|---|---|---|
| 仅图表派(阶段6基线) | 1.251 | 0.751 | 665 | 4/4 |
| +零智能 | 1.692 | 1.192 | 1395 | 4/4 |
| +基本面派 | 0.951 | 0.451 | 1765 | 4/4 |
| 三者全部 | 0.765 | 0.265 | 2493 | 4/4 |

- 阶段6 基线 k = 1.251
- **贡献最大的一类：三者全部**

⚠️ 「元订单数」是独立跑一场数出来的（`n_meta_started` 之和），用来证明**配置真的落到了主体身上**——只看配置文件传没传是不算数的（`MetaOrderMixin` 收不到 config 时会用默认值，而默认是**开着**的）。

### 3.2 EB.2 长记忆的「味道」

| 配置 | ACF(1) | 半衰期(lag) | 最小值 | 首次转负(lag) | 尾段均值 | ΣACF |
|---|---|---|---|---|---|---|
| 仅图表派元订单 | +0.0924 | 28 | -0.0002 | 481 | +0.0033 | +7.41 |
| 仅基本面派元订单 | +0.0838 | 24 | -0.0039 | 180 | -0.0007 | +3.46 |
| 仅零智能元订单 | +0.0903 | 24 | -0.0025 | 358 | -0.0001 | +5.45 |

判据：基本面派的方向锚定在启动那一刻的 premium，而 premium 本身均值回归 ⇒ 它的衰减应当**更快、更易出现反转**（半衰期更短）。

### 3.3 EB.4 新参数对长记忆的边际影响

| 主体类型 | p_meta_start | γ | ΣACF | 元订单数 |
|---|---|---|---|---|
| zero_intel | 0.005 | 1.3497 | +6.45 | 573 |
| zero_intel | 0.020 | 1.3892 | +8.86 | 1391 |
| zero_intel | 0.080 | 1.4754 | +13.29 | 2182 |
| fundamentalist | 0.005 | 1.3190 | +3.40 | 753 |
| fundamentalist | 0.020 | 1.2990 | +3.62 | 1757 |
| fundamentalist | 0.080 | 0.7884 | +2.76 | 2657 |

- zero_intel：γ 摆幅 0.1258，ΣACF 摆幅 6.85
- fundamentalist：γ 摆幅 0.5306，ΣACF 摆幅 0.85

⇒ 对**长记忆(ΣACF)** 影响更大的一类：**zero_intel**（决定后续校准优先级）。

- 但要注意：zero_intel 的 **γ 摆幅只有 0.1258**而 ΣACF 摆幅 6.85；
  fundamentalist 反过来——**γ 摆幅 0.5306**而 ΣACF 摆幅只有 0.85。
- ⇒ **两类影响的是不同的指标**：一个推动量（长记忆），一个推动深度剖面。「哪一类更重要」在**不指定指标**时没有唯一答案；校准优先级要按你想修的是 k 还是 γ 来定。

---

## 4. 任务书清单的完成情况

| 任务书条目 | 状态 | 说明 |
|---|---|---|
| A.1 修正离散化公式 + M34 | ✅ | **原以为是新工作，实测早已实现**（形状不同）；改为实测验证并钉死回归测试 + M34 |
| A.2 跑 EA.1（决定性） | ✅ | 并额外发现真正的根因（λ̄ 标尺），**结论与任务书预期不同** |
| A.3 深度对比工具 + M35 + EA.2 | ✅ | 工具并入 `tw/impact.py`（任务书点名的新文件不存在，按本项目惯例不新开文件） |
| A.4 方差匹配对照 EA.3 | ✅ | 改用**置换代理**（比任务书建议的正弦调制更干净） |
| A.5 单侧应用 EA.4 | ✅ | 需求侧 k=0.455，是 A 线最接近平方根律的一档 |

> ⚠️ **后续更正（回填标准配置后，见工作线 L / O）**：
> 三线深挖最初观察到「只稀疏化吃单方」(k=0.455) 比「双边稀疏化」(k=0.947) 更接近平方根律，据此提出「非对称原则」是修复冲击凹度的关键。
> 经过回填标准配置（8 种子、未饱和档位）重新测量，taker / both / maker 三个配置的 k 分别为 **0.734 / 0.794 / 0.739**，**极差只有 0.060**，而这个差异所需的分辨力（约 **1000 个种子**）远超过当前投入的规模（工作线 O 的区间重叠检验：三对两两比较的重叠都在 80% 以上）。
> 更准确的结论是：真正驱动冲击凹度改善的，是**「存在时间聚集性」这件事本身**（Hawkes 关闭→开启，k 从 1.292 到约 0.755，效应量 **0.536**）；聚集性具体作用在**需求侧还是供给侧**，影响相对次要——**效应量小一个数量级**（0.060 vs 0.536，约 9 倍）。
> 注：上面这些点估计**方向仍然一致**（taker 最小），只是统计上无法与噪声区分；被撤回的是「非对称配置更好」这个**结论性**说法，不是这些读数本身。
| A.6 EA.5 贡献量排序 | ✅ | 见 §1.7 裁决表 |
| B.1/B.2 两个 Meta 子类 + M36/M37 | ✅ | **按真实接口写成子类**（任务书的 `on_tick` 不存在，照抄会静默失效） |
| B.3 EB.1 覆盖面消融 | ✅ | 见 §3.1 |
| B.4 EB.2 长记忆味道 | ✅ | 见 §3.2 |
| B.5 EB.3 联合 Hawkes | ✅ | 已并入三线合并验证（§1.5 的 J3）：**在 B 之上叠加 A 的边际贡献是负的** |
| B.6 EB.4 参数敏感性 | ✅ | 见 §3.3 |
| C.1 EC.1 修复前后对比 + M39 | ✅ | 见 §2.1 |
| C.2 EC.2 基线回归 | ✅ | 逐位一致 |
| C.3 EC.3 配对反事实 + M38 | ✅ | 12 种子；**降噪倍数与任务书预期不符，已如实说明** |
| C.4 EC.4 相关性敏感性重测 | ✅ | 与 EC.3 同一次运行（它就是同一张表加配对） |
| 合并验证 | ✅ | 见 §1.5：J1–J3（k 检验）+ J4（阶段11 链条）。合并计划先写后跑：`docs/三线深挖-合并计划.md` |
| 全量回归 M1~M43 | ✅ | **43/43 全部被抓到 ✅**（含本轮新增的 M34~M43） |
| 成文后的收尾自查 | ✅ | 见 §5 末三条：**本轮自查又抓到三个真 bug**（M40/M41/M42），全部已修 + 钉死回归测试 |
| 生成本轮交付小结 | ✅ | 本文件 |

## 5. 诚实边界

- **E8.2 的 0.481 不应再被引用。** 它是标尺错误的产物；真实的聚集性效果是 k ≈ 0.947（b=0.6）。旧的报告文字与阶段小结需要同步更正。
- **EA.3 的结论（H2 是主因）建立在一个代理对照上。**置换保持了 ρ 的边际分布，但同时也抹掉了**任何**时间结构（不只是一阶自激）。所以更准确的说法是：**「ρ 的时间结构」对 k 不是必需的，「ρ 的波动幅度」才是**。
- **EA.4 的单侧结果有一个未排除的混淆**：三臂的活跃度虽然接近（81.89~82.44 成交/tick），但**稀疏化作用在不同主体集合上**，各自改变了不同的微观结构。要完全归因需要「同活跃度 + 只换作用面」的更强对照。
- **B 线没有做联合 Hawkes 测试（EB.3）**、也没有做三线合并验证——理由写在 §4 表里：A 线证明「标尺必须先统一」，在统一之前做联合只会把两类误差叠起来。**（本条已被 §1.5 的三线合并验证补上。）**
- ⭐ **合并验证暴露了一个结构性问题：`rho_to_count` 在 ρ̄=1 附近天然是「半哑火」的。**
  J3 的 ρ̄ = 1.0319（正确标定后本就该≈1），但**稀疏化生效只有 24.4%**——即七成以上的 tick 里 `round(ρ×300)` 都 ≥ 300、被夹到全部主体。
  原因不是标定错了：把 ρ 映射成「允许多少主体出手」时，**ρ>1 的那一半质量是实现不了的**（总不能超过全部主体）。于是这个机制**只能变少、不能变多**，在 ρ̄=1 处天然不对称。
  ⇒ **这是 Hawkes 集成的结构性限制，不是 E8.2 那次的标定事故。**要真正让机制全程生效，需要改映射（例如按 ρ 缩放**订单量**而不是**主体数**）。
- ⚠️ **A 在 B 之上的边际贡献是负的**：J2（三类覆盖）k=0.765 → J3（再叠加修正 Hawkes）k=0.957。**叠加没有帮助。**
- J4：联合配置下阶段11 链条成立环节数 = **4/6**，与二期原结果相同 ⇒ **联合配置不改变链条判定**。② B 的价格偏离在三颗种子上**恰好为 0**，即 A 的冲击完全没有传到 B。
- **EC.3 的降噪倍数为负向结果**（配对没有降噪），已在 §2.3 说明原因，未用话术掩盖。
- 本轮的实验脚本（`run_workstream_*.py`）都带 `--only`，可以分段重跑；产物写入 `out/workstream_*_metrics.json`（**合并写入**，不覆盖）。

### 5.1 成文之后的自查，又抓到三个真 bug（全部已修 + 已钉回归测试）

交付前按惯例把产物当「别人的东西」重读了一遍，抓到三个**都不会被「测试全绿 + 自检通过」发现**的缺陷。这里如实记下，因为它们的形态比单个 bug 更值得记：**失败是静默的，而检查项恰好不看那个地方。**

| 编号 | 缺陷 | 症状为什么阴 | 修法 |
|---|---|---|---|
| M40 | 写回合并计划时用了 `"".join(L)` 而不是 `"\n".join(L)` | **整节被拼成一行**，§5 的两张表在 markdown 里彻底不渲染；而脚本退出码 0、自检也不检查渲染 ⇒「跑成功了」与「产物能读」之间掉进去一次 | 改成 `"\n".join`；新增 `TestPlanDocIsRenderableMarkdown`（4 条，断言表格逐行独立 / 表头与分隔行列数一致 / 假设在结果之前 / 承重数字在文里） |
| M41 | `--only mutation --with-mutation --list` 打印「共 0 步」| `tests`/`mutation` 不在 `STEPS` 里（它们是收尾步骤），而追加逻辑只写在 `if only is None` 分支里 ⇒ **用户点名的步骤被静默丢掉**。与早先的 `--skip 8,9,11` 静默不跳是同一形态 | `--only` 也认这两个名字；点名了 mutation 但没给 `--with-mutation` 时**必须打印警告**（闸门正当，沉默不正当）；零步骤时显式提示。新增 6 条测试 |
| M42 | `--only` 路径跑完后在收尾打印处抛 `NameError: name 'skip' is not defined` | **所有步骤都跑完了、对账也通过了、test_count 也写回了**，唯独最后一行崩掉 ⇒ 退出码 1。**产物是对的，但程序说自己失败**——看退出码的人白重跑，看日志的人以为成功，两边都不对 | 用 `normalize_skip(args.skip)`；新增一条走通整个 `main()`（重活打桩）断言返回 0 的测试 |

三个都补了对应的变异体（M40/M41/M42），**逐个单独跑确认能变红**，再进全量。这一步不是形式：M40 与 M42 的第一次「红」都是被新写的断言在 **<1 秒**内抓到的，而在此之前它们是靠「没人去看渲染结果」活下来的。

⚠️ 顺带更正一个陈旧产物：`out/test_count.txt` 长期停在 **528**，而本轮实测是 **544**（新增了 A/B/C 三条线的回归测试）。它由 tests 步骤自己写回，但上一轮跑完 tests 之后又加了测试却没再跑 tests 步骤 ⇒ 报告引用的会是一个**看起来完全正常**的旧数字。本轮已重跑 tests 步骤对齐；另在自检里加了 ③b/③c 两项，让「手写常量与实测脱节」以后一定会被报出来。
