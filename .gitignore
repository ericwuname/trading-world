# ── 产物目录（约 500 MB，且可由脚本完整重建）────────────────────
# 包含：各阶段 metrics JSON、日志、变异验证沙箱（out/_mutation/）、
#       图片、重跑前快照（out/_archive/）。
# 一键复现：python scripts/reproduce_all.py
out/

# ── 行情库（本地缓存，可由 data_cli 重建）───────────────────────
# candles 是拉取/生成出来的派生物，随用随建。
# ⚠️ 但**不要**忽略 fetches 账本所依赖的任何东西：
#    「这份数据是什么时候、用什么参数拿的」这个问题必须事后可答，
#    所以账本随库一起存在于本地即可 —— 需要长期留存的证据应另行归档。
# 重建：python scripts/data_cli.py build --inst BTC-USDT-SWAP
data/market.sqlite
data/market.sqlite-wal
data/market.sqlite-shm

# ── Python ──────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ── 本地环境与编辑器 ─────────────────────────────────────────────
.venv/
venv/
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 密钥（⚠️ 永远不要进仓库）─────────────────────────────────────
# 本项目的 LLM 通道（Agnes / Gemini / 智谱）与交易所凭证都放在
# 仓库**外面**（~/.workbuddy/secrets/ 与用户级记忆），这条是兜底：
# 万一有人在项目里临时建了个 .env / key 文件，误 commit 时能挡住。
.env
.env.*
secrets/
*.key
*_key.txt
credentials.json

# ⚠️ **项目内的 `.workbuddy/`（环境状态：记忆 / automation 缓存）不进仓库**
# 仓库是 public；记忆内容还会随时间长，不该混进版本历史。
# （上面那条只覆盖**用户级**的 `~/.workbuddy/`，不覆盖项目内这份。）
.workbuddy/
